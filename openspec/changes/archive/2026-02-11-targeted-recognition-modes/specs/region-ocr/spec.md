## REMOVED Requirements

### Requirement: Shift+draw triggers OCR recognition
**Reason**: Replaced by keyboard-toggled recognition modes (`n` for neume, `t` for text) that provide more explicit, targeted control over which recognition pipeline to invoke.
**Migration**: Users press `n` or `t` to enter a recognition mode, then draw a rectangle. This replaces holding Shift while drawing.
